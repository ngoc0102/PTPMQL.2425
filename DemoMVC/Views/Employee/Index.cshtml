@model IEnumerable<DemoMVC.Models.Employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tuổi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nghềnghiep)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hoten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quequan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tuổi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nghềnghiep)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hoten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quequan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CCCD">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CCCD">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CCCD">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
